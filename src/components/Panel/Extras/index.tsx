import Section, { SectionC } from "../Section"
import Birthday from "./Birthday"

const Extras = () => {
  return <Section>
    <SectionC>
      <Birthday/>
    </SectionC>
  </Section>
}

export default Extras